import React from 'react'
import { useAuth } from '../Auth/AuthProvider'

function User() {
    const { auth } = useAuth()

    // if (User)
    // console.log('')
    //make a call to /user/:id
}

export default User